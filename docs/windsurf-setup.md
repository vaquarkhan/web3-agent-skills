# Windsurf Setup

```bash
scripts/install.sh --tool windsurf --target /path/to/project
```

Copies `.windsurfrules.example` — rename or merge into your Windsurf rules.

See `AGENTS.md` and `guardrails/web3-guardrails.yaml`.
