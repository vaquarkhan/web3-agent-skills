# Claude Setup

1. Clone or install toolkit into project
2. Use `.claude/commands/` slash commands: `/simulate`, `/audit`, `/screen`, `/swap`, `/bridge`, `/review`
3. Configure MCP from `mcp/` (build servers first: `npm run build`)
4. Load `CLAUDE.md` and `AGENTS.md` for routing
